import { APP_NAME } from "@/config/app";

const BANNER_HEADER = `Visualize your ideas with ${APP_NAME}`;
const BANNER_DESC =
  "Turn inspiration into design in no time. Simply upload an image and AI do the rest";

export { BANNER_DESC, BANNER_HEADER };
